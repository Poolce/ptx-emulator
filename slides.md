# План презентации
**«Создание инструмента для эмуляции исполнения CUDA PTX ядер на центральном процессоре»**
Мусин А.Н. · 3824М1ПМвм · Руководитель: доц. к.т.н. Горшков А.В.

---

## Оформление (единое для всех слайдов)

- **Фон:** белый / светло-серый
- **Текст:** тёмно-синий (`#1A237E`) или чёрный
- **Акцент:** тёмно-зелёный (`#1B5E20`) для ключевых терминов
- **Заголовок:** 40 пт, полужирный
- **Основной текст:** 24–28 пт
- **Дополнительный текст / нижний колонтитул:** 14–16 пт
- **Нижний колонтитул (слайды 2–10):** «Мусин А.Н. · Эмулятор CUDA PTX · слайд N»
- **Анимация:** нет (только смена слайдов)

---

## Слайд 1 — Титульный (0:00–0:30)

**Заголовок:** Создание инструмента для эмуляции исполнения CUDA PTX ядер на центральном процессоре

**Содержание:**
```
ННГУ им. Н.И. Лобачевского
Институт ИИТММ · Кафедра ВВСП

МАГИСТЕРСКАЯ ДИССЕРТАЦИЯ

Выполнил: студент гр. 3824М1ПМвм  Мусин А.Н.
Научный руководитель: доц. к.т.н.  Горшков А.В.

Нижний Новгород, 2026
```

**Речь (~30 с):** «Добрый день. Тема моей магистерской диссертации — создание программного эмулятора, позволяющего исполнять CUDA PTX ядра на обычном процессоре без видеокарты.»

---

## Слайд 2 — Актуальность и проблема (0:30–1:30)

**Заголовок:** Проблема: GPU-код без GPU

**Содержание (маркированный список, 6 строк):**
```
• CUDA PTX — промежуточное представление GPU-кода NVIDIA
• Разработка и тестирование требуют физической GPU
• Облачный CI/CD: GPU-ресурс дорог и недоступен постоянно
• Существующие инструменты устарели (PTX ISA ≤ 4.x)
  или не дают профилировочных данных

Цель: эмулятор PTX ISA 7.x с профилировщиком
      и прозрачным перехватом без правки кода
```

**Визуал:** таблица сравнения инструментов (Ocelot / GPGPU-Sim / Compute Sanitizer / данная работа) — из ch2_theory.tex, tab:related_work

**Речь (~60 с):** «CUDA PTX — это байт-код GPU. Чтобы запустить GPU-ядро, обычно нужна видеокарта. Это создаёт проблему в CI/CD и при обучении. Существующие эмуляторы поддерживают устаревшие версии ISA и лишены профилировщика. Цель работы — закрыть этот пробел.»

---

## Слайд 3 — Цель и задачи (1:30–2:15)

**Заголовок:** Цель и задачи работы

**Содержание (2 колонки):**
```
ЦЕЛЬ                          7 ЗАДАЧ
Программный эмулятор          1. Перехват CUDA Runtime API
CUDA PTX ISA 7.x              2. Синтаксический анализатор PTX
на CPU, без GPU,              3. Таблица диспетчеризации
с профилировщиком             4. Исполнители инструкций
                              5. Алгоритм PDOM-реконвергенции
Критерий готовности:          6. Система профилирования
все 7 интеграционных          7. Инструмент генерации отчётов
ядер проходят при
погрешности ≤ 10⁻³
```

**Речь (~45 с):** «Сформулировано семь задач. Ключевая техническая проблема — корректная обработка дивергентных потоков варпа при ветвлении: когда разные потоки идут по разным ветвям if-else, эмулятор обязан исполнить их в правильном порядке.»

---

## Слайд 4 — Архитектура инструмента (2:15–3:15)

**Заголовок:** Архитектура эмулятора

**Визуал (схема — 80% слайда):**
```
┌─ cuemu (CLI) ──────────────────────────────────────────┐
│  LD_PRELOAD=libemuruntime.so  ./cuda_app              │
└────────────────────┬───────────────────────────────────┘
                     │ CUDA Runtime API calls
         ┌───────────▼────────────┐
         │   libemuruntime.so     │  ← Перехват
         │  (CUDA Runtime Stub)   │
         └──────┬──────┬──────────┘
                │      │
        ┌───────▼──┐ ┌─▼──────────┐
        │  Parser  │ │  Executor  │
        │  PTX→IR  │ │  SIMT+PDOM │
        └───────┬──┘ └─┬──────────┘
                │      │
         ┌──────▼──────▼──────┐
         │    Profiler         │
         │  4 метрики → HTML  │
         └─────────────────────┘
```

**Подпись схемы:** «Рисунок 1. Архитектура эмулятора cuemu»

**Речь (~60 с):** «Инструмент состоит из пяти компонентов. Утилита cuemu устанавливает LD_PRELOAD и запускает приложение. Библиотека-заглушка перехватывает вызовы CUDA Runtime. Парсер строит таблицу инструкций из PTX-текста. Исполнитель реализует семантику SIMT. Профилировщик собирает метрики.»

---

## Слайд 5 — LD_PRELOAD и парсер PTX (3:15–4:15)

**Заголовок:** Перехват и разбор PTX

**Содержание (2 блока):**
```
ПЕРЕХВАТ (LD_PRELOAD)
• libemuruntime.so подставляется перед libcudart.so
• 8 перехватываемых функций: cudaLaunchKernel,
  cudaMalloc, cudaMemcpy, cudaMemset, …
• PTX извлекается через cuobjdump из fatbinary
• Без правки бинарного файла приложения

ОДНОПРОХОДНЫЙ ПАРСЕР PTX ISA 7.x
• 66 мнемоник, сложность O(N) по числу инструкций
• Регулярные выражения генерируются скриптом
  autogen.py из instructions.json
• Таблица диспетчеризации: O(1) поиск инструкции
```

**Речь (~60 с):** «Механизм LD_PRELOAD позволяет подменить CUDA Runtime без изменения кода приложения — это ключевое требование для CI. Парсер является однопроходным: он читает PTX один раз и строит вектор объектов инструкций, готовых к исполнению.»

---

## Слайд 6 — Модель исполнения SIMT и дивергенция (4:15–5:30)

**Заголовок:** Исполнение: SIMT и PDOM-реконвергенция

**Визуал (схема ветвления + формула, 60% слайда):**
```
Варп (32 потока), маска M = 0xFFFFFFFF

        @%p bra $L_taken
       /                \
  M_taken              M_not
  = M ∧ P              = M ∧ ¬P
  → $L_taken           → PC+1

      PDOM_Diverge: push {IPDOM, M_full, is_conv=false}
                        |
                    IPDOM(N)   ← реконвергенция
                        |
                   M восстановлен
```

**Формула (выделенная строка):**
```
IPDOM(N) = argmin { D : D pdom N, D ≠ N }
```

**Текст (4 строки):**
```
• При ветвлении варп разбивается на два подмножества потоков
• Стек дивергенции сохраняет маски и адреса реконвергенции
• Алгоритм PDOM_Merge восстанавливает полную маску M
• Корректность доказана для вложенных ветвлений (глубина k ≥ 2)
```

**Речь (~75 с):** «Самая сложная часть — дивергентные потоки. В GPU все 32 потока варпа исполняют одну инструкцию. При if-else часть потоков идёт в одну ветвь, часть — в другую. Эмулятор должен исполнить обе ветви последовательно с правильными масками. Алгоритм PDOM находит точку реконвергенции через теорию постдоминаторов.»

---

## Слайд 7 — Система профилирования (5:30–6:30)

**Заголовок:** Профилировщик: 4 метрики и HTML-отчёт

**Содержание (таблица 4 строки + рисунок):**

| Метрика | Формула | Смысл |
|---|---|---|
| branch_efficiency | η = popcount(M) / 32 | Доля активных потоков |
| bank_conflicts | C = max\|{t : bank(ea(t))=b}\| − 1 | Конфликты банков памяти |
| global_coalescing | κ = T_ideal / T_actual | Слияние транзакций |
| write_ops | W[τ] = Σ popcount(M_k) | Давление на регистры |

```
Архитектура профилировщика:
profiling_metrics.json → autogen.py → Profile() в каждой инструкции
[[unlikely]] защита → нулевые накладные расходы на горячем пути
Буферизация per-warp → сброс с мьютексом 1 раз на блок
```

**Речь (~60 с):** «Профилировщик собирает четыре метрики уровня инструкций. Код генерируется автоматически из JSON-конфигурации. Атрибут unlikely гарантирует нулевые накладные расходы при выключенном профилировании.»

---

## Слайд 8 — Тестирование (6:30–7:30)

**Заголовок:** Верификация: 7 интеграционных ядер

**Содержание (таблица 7 строк):**

| Ядро | Паттерн | Погрешность | Замедление vs V100 |
|---|---|---|---|
| vadd_custom | Глобальная память | < 10⁻⁶ | — (базовый) |
| gelu_custom | Трансцендентные функции | < 10⁻⁴ | — |
| softmax_custom | bar.sync, редукция | < 10⁻⁴ | — |
| mmul_custom | Разделяемая память, тайлинг | < 10⁻³ | 1 900–2 300× |
| attention_custom | Flash Attention, вложенные циклы | < 10⁻³ | — |
| conj_grad | Динамическое число итераций | < 10⁻³ | — |
| fft_custom | Битреверс, разделяемая память | < 10⁻³ | — |

```
Покрытие кода: строки 80.4% · ветви 78.7% (lcov)
Тестовая машина: AMD Ryzen 7 3700U, 8 ГБ RAM, без GPU
```

**Речь (~60 с):** «Все семь ядер прошли верификацию. Максимальное отклонение от CPU-эталона не превышает десятитысячной доли. Замедление 1900–2300× относительно V100 — неизбежная цена программной эмуляции. Для CI и разовых анализов это приемлемо: нетривиальные ядра на GPU работают сотни микросекунд.»

---

## Слайд 9 — Демонстрация HTML-отчёта (7:30–8:15)

**Заголовок:** Пример HTML-отчёта профилировщика

**Визуал (скриншот profiler_html.png — 85% слайда):**
Подпись: «Рисунок 2. HTML-отчёт для ядра fft_custom: панель метрик, тепловая карта инструкций, аннотированный исходный код»

**Текст (4 строки под картинкой):**
```
• Сводные метрики ядра (pills-панель)
• Таблица инструкций с тепловой подсветкой по exec_count
• Топ-10 горячих точек по дивергенции и конфликтам банков
• Аннотированный исходный .cu-файл через директиву .loc
```

**Речь (~45 с):** «Результат профилирования — самодостаточный HTML-файл. Здесь видны сводные метрики, тепловая карта инструкций и аннотированный исходный код с метриками на каждой строке.»

---

## Слайд 10 — Заключение (8:15–9:00)

**Заголовок:** Заключение и дальнейшие направления

**Содержание (2 блока):**
```
ДОСТИГНУТЫЕ РЕЗУЛЬТАТЫ
✓ 7 из 7 задач выполнены
✓ PTX ISA 7.x: 66 мнемоник
✓ Погрешность ≤ 10⁻³ на всех ядрах
✓ Прозрачный перехват — без правки кода
✓ HTML-отчёт с 4 метриками уровня инструкций
✓ Апробация в курсе «Новые архитектуры»

НАПРАВЛЕНИЯ РАЗВИТИЯ
→ Тензорные инструкции: mma, ldmatrix
→ JIT-компиляция (LLVM) для снижения замедления
→ Полное покрытие модификаторов ISA
```

**Речь (~45 с):** «В работе создан функциональный эмулятор CUDA PTX, решающий все поставленные задачи. Инструмент апробирован в учебном процессе. Основное направление развития — поддержка тензорных инструкций и JIT-компиляция для повышения скорости. Спасибо за внимание.»

---

## Хронометраж

| Слайд | Тема | Время |累積 |
|-------|------|-------|------|
| 1 | Титульный | 0:30 | 0:30 |
| 2 | Актуальность | 1:00 | 1:30 |
| 3 | Цели и задачи | 0:45 | 2:15 |
| 4 | Архитектура | 1:00 | 3:15 |
| 5 | LD_PRELOAD + парсер | 1:00 | 4:15 |
| 6 | SIMT + PDOM | 1:15 | 5:30 |
| 7 | Профилировщик | 1:00 | 6:30 |
| 8 | Тестирование | 1:00 | 7:30 |
| 9 | HTML-отчёт | 0:45 | 8:15 |
| 10 | Заключение | 0:45 | 9:00 |

**Итого: 9:00 (в пределах 8–10 мин)**

---

## Раздаточный материал для ГЭК

Рекомендуется распечатать слайды 4, 6, 8 (архитектура, PDOM-схема, таблица тестов).
