# Данные для диаграммы производительности (PPTX)

Источник: таблица `tab:integration_results`, [report/chapters/ch8_testing.tex](report/chapters/ch8_testing.tex).
Все значения приведены к **миллисекундам** (V100 в отчёте дан в мкс — переведён в мс),
чтобы поместить три ряда на один график.

## Данные (вставить в Excel / PowerPoint)

| Ядро | Эмулятор без проф., мс | Эмулятор со сбором проф., мс | V100-PCIE, мс |
|---|---|---|---|
| vadd | 20.5 | 34.8 | 0.0072 |
| gelu | 46.8 | 121.8 | 0.0080 |
| softmax | 60.9 | 268.4 | 0.0078 |
| fft | 52.6 | 218.0 | 0.0089 |
| mmul | 111.7 | 970.5 | 0.0178 |
| attention | 300.5 | 2074.1 | 0.1319 |
| conj_grad | 890.6 | 1879.7 | 0.4763 |

## Настройка графика в PowerPoint

1. **Тип:** «Гистограмма с группировкой» (Clustered Column) — 3 ряда на каждое ядро.
2. **Ось Y → логарифмическая:** двойной клик по оси → «Формат оси» → ☑ «Логарифмическая шкала», основание 10.
   Без этого столбцы V100 (≈ 0.007 мс) будут невидимы рядом с эмулятором (до 2074 мс).
3. **Границы оси Y:** мин. `0.001`, макс. `10000`.
4. **Подпись оси Y:** «Время исполнения, мс (лог. шкала)».
5. **Легенда:** Эмулятор (без проф.) · Эмулятор (со сбором проф.) · V100-PCIE.

## Справочно — исходные значения V100 в мкс

| Ядро | V100, мкс |
|---|---|
| vadd | 7.2 |
| gelu | 8.0 |
| softmax | 7.8 |
| fft | 8.9 |
| mmul | 17.8 |
| attention | 131.9 |
| conj_grad | 476.3 |

---

# Непосредственный постдоминатор (IPDOM)

**Определение.** Непосредственный постдоминатор узла $N$:

$$
\mathrm{IPDOM}(N) = \operatorname*{argmin}_{D \,:\, D \,\mathrm{pdom}\, N,\; D \ne N} \bigl| E^{*}[N, D] \bigr|
$$

## Что означают переменные

| Обозначение | Смысл |
|---|---|
| $N$ | Рассматриваемый узел графа потока управления (CFG) — базовый блок, для которого ищется непосредственный постдоминатор. |
| $D$ | Узел-кандидат, который постдоминирует $N$. |
| $D \,\mathrm{pdom}\, N$ | Отношение **постдоминирования**: $D$ постдоминирует $N$, если **любой** путь из $N$ до выходного узла графа обязательно проходит через $D$. |
| $D \ne N$ | Кандидат не совпадает с самим $N$ (рассматриваются только *строгие* постдоминаторы). |
| $E^{*}[N, D]$ | Множество рёбер на пути от $N$ до $D$ в графе. |
| $\lvert E^{*}[N, D] \rvert$ | Мощность этого множества — число рёбер (длина пути) от $N$ до $D$. |
| $\operatorname{argmin}$ | Выбор того кандидата $D$, который **минимизирует** $\lvert E^{*}[N, D] \rvert$. |

## Смысл формулы

Среди всех строгих постдоминаторов узла $N$ выбирается **ближайший** —
тот, путь до которого содержит наименьшее число рёбер. Именно в этой точке
расходившиеся после ветвления потоки управления гарантированно сходятся
снова, поэтому $\mathrm{IPDOM}(N)$ используется как точка реконвергенции
варпа при обработке дивергенции.
