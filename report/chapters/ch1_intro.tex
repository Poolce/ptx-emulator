\section{Введение}

\subsection{Актуальность и мотивация исследования}

Графические ускорители класса NVIDIA GPU заняли центральное место
в современных высокопроизводительных вычислениях: от обучения крупных
языковых моделей до физического моделирования и научных расчётов.
Программирование GPU на платформе CUDA строится на модели SIMT
(Single Instruction, Multiple Threads), в которой группы из 32~потоков~---
варпы~--- исполняют одну и ту же инструкцию над различными данными~\cite{Lindholm2008, Nickolls2008}.
Ключевым промежуточным представлением в данной экосистеме служит PTX
(Parallel Thread Execution)~--- аппаратно-независимый ассемблер NVIDIA~\cite{PTXISA},
в который компилируется CUDA~C++ и из которого генерируется машинный
код SASS для конкретного поколения GPU.

Несмотря на широкое распространение CUDA, отладка и профилирование
CUDA-ядер по-прежнему сопряжены с существенными ограничениями.
Официальные инструменты NVIDIA (Nsight, Compute Sanitizer) требуют
наличия физического GPU, что делает их недоступными в типичных
облачных средах непрерывной интеграции, виртуальных машинах и
академических лабораториях с ограниченным бюджетом. Помимо этого,
аппаратные счётчики производительности предоставляют агрегированную
статистику на уровне SM, однако не обеспечивают детальных метрик
на уровне отдельных инструкций PTX и строк исходного кода~---
что необходимо для углублённого анализа эффективности алгоритмов.

Программный эмулятор PTX~ISA позволяет устранить оба ограничения
одновременно: функционировать на стандартном CPU без GPU-оборудования и
собирать произвольные метрики уровня инструкций. Это открывает следующие
сценарии применения:

\begin{enumerate}
  \item \textbf{Среды непрерывной интеграции без GPU.} Автоматическая
        проверка корректности CUDA-ядер при каждом коммите без
        специализированного оборудования.
  \item \textbf{Академические исследования.} Изучение архитектурных
        свойств GPU-программ без доступа к аппаратным счётчикам.
  \item \textbf{Разработка и отладка алгоритмов.} Детерминированное
        пошаговое исполнение с полным контролем над состоянием варпа.
  \item \textbf{Образовательные задачи.} Наглядное представление
        механизмов дивергенции, слияния транзакций памяти и конфликтов
        банков для изучения принципов GPU-архитектуры.
\end{enumerate}

Таким образом, программный эмулятор PTX позволяет решать задачи как
обеспечения корректности (сценарии 1 и 3), так и углублённого понимания
архитектурного поведения программ (сценарии 2 и 4)~--- два взаимодополняющих
аспекта, которые ни один из существующих инструментов не охватывает в совокупности.

\subsection{Анализ существующих решений}

На сегодняшний день существует ряд инструментов, частично решающих
задачу функциональной эмуляции CUDA-ядер.

\textbf{Ocelot}~\cite{Diamos2010}~--- один из первых функциональных
PTX-интерпретаторов с поддержкой перехвата через CUDA~Runtime~API.
Его ключевое ограничение~--- привязка к PTX~ISA версий 1.x--2.x (CUDA~4.x)
и отсутствие поддержки директив PTX~7.x+, в частности инструкций
\texttt{ldmatrix}, \texttt{wmma} и атрибута \texttt{.address\_size~64}.
Проект не поддерживается с 2013~года.

\textbf{GPGPU-Sim}~\cite{Bakhoda2009}~--- микроархитектурный симулятор,
воспроизводящий конвейер SM с точностью до цикла (timing-accurate).
Его цель принципиально отличается от задачи данной работы: GPGPU-Sim
оценивает латентности и пропускную способность, тогда как функциональный
эмулятор нацелен на корректность результата и профилирование
на уровне инструкций. Время одного запуска в GPGPU-Sim превышает
время в функциональном эмуляторе на порядки.

\textbf{CUDA Compute Sanitizer}~--- официальный инструмент NVIDIA
для обнаружения гонок данных, ошибок памяти и нарушений барьерной
синхронизации. Требует наличия физического GPU и не предоставляет
метрик уровня инструкций PTX.

Проведённый анализ выявляет исследовательскую нишу: не существует
актуального (PTX~ISA~7.x+), расширяемого функционального эмулятора
с интегрированным профилировщиком и механизмом прозрачного перехвата
CUDA~Runtime~API без модификации бинарного файла приложения.
Настоящая работа заполняет данную нишу.

Каждый из перечисленных инструментов закрывает лишь часть
требуемого пространства свойств: Ocelot сочетал работу без GPU
с поддержкой PTX и профилированием, однако не поддерживается с 2013~года
и ограничен ISA~2.x; GPGPU-Sim работает без GPU, но ориентирован
на моделирование производительности микроархитектуры, а не на функциональный
эмулятор с метриками уровня инструкций; Compute Sanitizer предоставляет
аналитику корректности, но принципиально требует физического GPU.
Таким образом, среди актуально поддерживаемых инструментов отсутствует
решение, объединяющее поддержку PTX~ISA~7.x, работу без GPU и
профилирование на уровне отдельных инструкций в рамках единого инструмента.

\subsection{Цели и задачи}

\textbf{Цель работы}~--- разработка программного эмулятора PTX~ISA,
обеспечивающего функционально корректное исполнение CUDA-ядер без GPU
с возможностью сбора детальных метрик производительности на уровне
инструкций.

Выбор PTX~ISA в качестве уровня эмуляции обусловлен тем, что PTX
является стабильным публичным контрактом NVIDIA~\cite{PTXISA}: компилятор гарантирует
его синтаксическую и семантическую совместимость между поколениями GPU,
тогда как SASS~--- бинарный машинный код~--- документируется значительно хуже
и изменяется с каждым поколением архитектуры.

Для достижения указанной цели поставлены следующие \textbf{задачи}:
\begin{enumerate}
  \item Реализовать механизм прозрачного перехвата CUDA~Runtime~API
        через \texttt{LD\_PRELOAD} с извлечением PTX посредством \texttt{cuobjdump}.
  \item Разработать однопроходный инкрементный синтаксический анализатор
        PTX-текста, поддерживающий подмножество PTX~ISA~7.x.
  \item Построить таблицу диспетчеризации и исполнители инструкций
        с формальной операционной семантикой.
  \item Реализовать алгоритм PDOM-реконвергенции для управления
        дивергентным потоком варпа.
  \item Разработать систему метрик профилировщика с генерацией кода
        по JSON-манифесту.
  \item Реализовать инструмент генерации самодостаточного HTML-отчёта
        с аннотацией строк исходного кода.
  \item Верифицировать корректность эмулятора на семи интеграционных
        тестах с реальными CUDA-ядрами.
\end{enumerate}

Задачи 1--2 обеспечивают прозрачность инфраструктуры запуска (пользователь
не изменяет свой код), задачи 3--4 образуют ядро исполнительного механизма,
задача 5--6 формируют наблюдаемость, а задача~7 подтверждает совокупную
корректность реализации на реалистичных нагрузках.

\subsection{Научная новизна}

Научная новизна работы состоит в следующем:

\begin{enumerate}
  \item Предложен и реализован прозрачный механизм перехвата
        CUDA~Runtime~API через \texttt{LD\_PRELOAD}, не требующий
        модификации бинарного файла приложения, с формальным
        обоснованием порядка разрешения символов динамическим
        загрузчиком.
  \item Разработан однопроходный PTX-анализатор с верифицированной
        корректностью на десяти CUDA-ядрах и 31~модульном тесте.
  \item Сформулирована формальная операционная семантика подмножества
        PTX~ISA, обеспечивающая верифицируемую корректность реализации
        исполнителей инструкций.
  \item Предложена архитектура системы метрик с генерацией кода, при
        которой добавление новой метрики сводится к правке JSON-манифеста
        без изменения кода на C++.
  \item Дана формальная модель вычисления коэффициента слияния
        \(\kappa = T_{\mathrm{ideal}} / T_{\mathrm{actual}}\) для
        транзакций глобальной памяти с учётом размера элемента.
\end{enumerate}

Совокупность перечисленных результатов представляет собой целостный вклад:
формальное обоснование механизма перехвата (пп.~1), верифицированный
синтаксический анализатор (пп.~2), операционная семантика (пп.~3)
и архитектурно обоснованная система метрик (пп.~4--5) образуют
взаимосвязанный набор теоретических и инженерных решений.

\subsection{Практическая значимость}

Разработанный инструмент имеет непосредственное практическое применение:

\begin{itemize}
  \item \textbf{Интеграция в конвейеры непрерывной интеграции.}
        Эмулятор запускается как обычный Linux-процесс; для включения
        в \texttt{GitHub Actions} или \texttt{Jenkins} достаточно
        однострочного скрипта \texttt{cuemu ./binary}.
  \item \textbf{Детальное профилирование.} HTML-отчёт с аннотацией
        строк исходного кода позволяет определить «горячие» инструкции
        и оптимизировать шаблоны обращения к памяти без запуска на GPU.
  \item \textbf{Образовательная ценность.} Пошаговая трассировка
        маски дивергенции обеспечивает наглядную демонстрацию
        механизма SIMT-исполнения.
\end{itemize}

Практическая ценность инструмента определяется прежде всего его
применимостью без специализированного оборудования: единственным
условием является наличие набора инструментов NVIDIA (в частности,
утилиты \texttt{cuobjdump}) для извлечения PTX-текста, тогда как
само исполнение ядер осуществляется полностью на CPU.

\newpage
